package com.ohgiraffers.section01.user_type;

public class Member {
    /*
    * 클래스 내부에 변수 선언이 가능
    * 전역변수: 클래스 내부에 선언된 변수는 클래스 전역에서 사용 가능 (== 필드 == 인스턴스 변수 == 클래스의 속성)
    * */

    String id;
    String pw;
    String name;
    int age;
    char gender;
    String[] hobby;
}
